<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.todo.app.mapper.TodoMapper">

	<select id="selectAll" resultType="com.todo.app.entity.Todo">
		SELECT * FROM todo_items
	</select>

	<select id="selectIncomplete" resultType="com.todo.app.entity.Todo">
		SELECT * FROM todo_items WHERE done_flg = false
	</select>

	<select id="selectComplete" resultType="com.todo.app.entity.Todo">
		SELECT * FROM todo_items WHERE done_flg = true
	</select>

	<insert id="add" parameterType="com.todo.app.entity.Todo"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO todo_items (
			title, time_limit, done_flg, memo, photo, priority, category_id
		) VALUES (
			#{title}, #{time_limit}, #{done_flg},
			#{memo}, #{photo}, #{priority}, #{categoryId}
		)
	</insert>

	<update id="update">
		UPDATE todo_items SET
			title = #{title},
			time_limit = #{time_limit},
			done_flg = #{done_flg}
		WHERE id = #{id}
	</update>

	<delete id="delete">
		DELETE FROM todo_items WHERE done_flg = true
	</delete>

</mapper>
